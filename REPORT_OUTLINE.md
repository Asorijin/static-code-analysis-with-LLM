# 项目报告提纲：基于LLM的静态代码分析与漏洞检测

> **数据来源说明**
> - `[代码]` = 从源码确认
> - `[实测]` = check_point.py 运行结果
> - `[配置]` = config.py 配置确认
> - `[待填]` = 需补充的空缺

---

## 一、项目概述

### 1.1 项目背景与目标
- **课程要求**：使用AI方法实现静态测试技术（Assignment 1, 40分, 截止4月20日）
- **研究目标**：利用LLM进行静态代码分析与漏洞检测

### 1.2 系统架构

系统采用三阶段Pipeline：

```
输入(Patch/代码) → CCI分析 → RAG检索 → CAVFD判断 → 输出
```

#### 核心处理阶段

| 阶段 | 全称 | 源码位置 | 功能 |
|------|------|----------|------|
| CCI | Code Change Intention | `utils.py:83-88` | 分析代码变更的意图、目的与影响（三维度：Code Change Summary / Purpose / Implications） |
| RAG | Retrieval-Augmented Generation | `utils.py:135-143` | 从历史漏洞向量数据库检索相似案例 |
| CAVFD | Combined Analysis for Vulnerability Fix Detection | `utils.py:113-121` | 综合判断是否为漏洞修复 |

#### 关键文件清单

| 文件 | 行数 | 职责 |
|------|------|------|
| `my_build_rag.py` | 83 | RAG知识库构建：从NVD获取CVE信息，生成CCI，向量化入库ChromaDB |
| `my_main.py` | 104 | 漏洞检测主流程：读取`testing_codebase.parquet`，逐条分析，输出`fin.csv` |
| `tool.py` | 165 | 交互式CLI工具：支持直接输入代码或从文件读取 |
| `check_point.py` | 62 | 评估模块：计算TP/FP/TN/FN及Precision/Recall/F1/MCC |
| `prompts.py` | 204 | 所有LLM提示词定义（CCI/SA/CAVFD各两种变体） |
| `utils.py` | 143 | 核心工具函数（API调用、向量化、RAG检索） |
| `config.py` | 53 | 配置类：API密钥、模型参数、路径配置 |

### 1.3 技术栈

| 组件 | 配置值 | 来源 |
|------|--------|------|
| LLM模型 | `qwen-turbo-1101` | [配置]`config.py:15` |
| 向量化模型 | `text-embedding-v4` | [配置]`config.py:19` |
| 向量数据库 | ChromaDB (SQLite, 16MB) | [实测]`chroma_db/chroma.sqlite3` |
| Collection名 | `three_aspect_summary_collection_gte-Qwen2-7B-instruct` | [配置]`config.py:30` |
| RAG知识库规模 | **831条** 历史漏洞记录 | [实测] `chroma_db.count()` |
| CVE数据源 | NVD API `services.nvd.nist.gov` | [配置]`config.py:47` |
| 测试数据集 | `testing_codebase.parquet`, 201条 | [实测] |
| 评估数据集 | `testcode.xlsx`, 583条 (label=1: 13, label=0: 570) | [实测] |

---

## 二、实验分析

### 2.1 准确性分析

#### 实验结果（[实测] `check_point.py`）

| 指标 | 值 | 说明 |
|------|-----|------|
| **TP** | 4 | 正确识别为漏洞 |
| **FP** | 1 | 误报（非漏洞判为漏洞） |
| **TN** | 569 | 正确识别为非漏洞 |
| **FN** | 9 | 漏报（漏洞判为非漏洞） |
| **Precision** | **0.8000** | TP/(TP+FP) |
| **Recall** | **0.3077** | TP/(TP+FN) |
| **F1 Score** | **0.4444** | 2·P·R/(P+R) |
| **MCC** | **0.4899** | Matthews相关系数 |

#### 评估方法说明
- **预测逻辑**（[代码]`check_point.py:12-13`）：
  - `"yes"` in cavfd → 预测为漏洞
  - `"no"` in cavfd → 预测为非漏洞
- **数据匹配**：`testcode.xlsx` 的 `cavfd` 列包含模型输出，`label` 列为真实标签

#### 结果分析
- **Precision 80%**：在预测为漏洞的样本中，80%确为漏洞
- **Recall 30.8%**：仅检测出13个漏洞中的4个，漏报严重
- **F1 44.4%**：综合评价指标偏低
- **MCC 0.49**：介于随机与完美之间，中等相关性

> **待补充**：需分析FN（漏报）的9个样本，探究漏报原因

### 2.2 覆盖率分析

| 维度 | 当前状态 | 数据来源 |
|------|----------|----------|
| 支持语言 | 仅Java（默认） | [配置]`config.py:33` |
| 输入格式 | patch/diff 或 原始代码 | [代码]`my_main.py:8-19` |
| RAG知识库 | 831条 Java CVE记录 | [实测] |
| 测试样本 | 201条 patch | [实测] |

> **待补充**：其他语言支持情况、C/C++/Python数据集准备情况

### 2.3 可泛化性分析

**支撑证据**：
- RAG机制：通过向量相似度检索历史漏洞，实现跨项目泛化
- Collection名称含`Qwen2-7B-instruct`：使用大语言模型生成CCI，具备语义理解能力

**局限性**：
- 当前仅Java漏洞数据，其他语言需重建RAG知识库
- Top_K=1（[配置]`config.py:34`）：仅检索最相似案例，可能引入偏差

> **待补充**：跨项目/跨语言泛化实验数据

---

## 三、项目报告

### 3.1 与传统非AI技术的对比

#### 传统静态分析工具（以SonarQube/FindBugs为例）

| 特征 | 传统工具 | 本项目 | 结论来源 |
|------|----------|--------|----------|
| 检测方式 | 基于规则/模式匹配 | 基于语义理解+知识检索 | [代码]整体架构 |
| 知识来源 | 内置规则库（需人工维护） | 动态RAG知识库（可扩展） | `my_build_rag.py` |
| 误报率 | 较低（规则精确） | **较高**（P=0.8但R=0.31） | [实测] |
| 漏报率 | 规则未覆盖则漏报 | **较低**（R=0.31说明漏报多） | [实测] |
| 多语言支持 | 需独立规则集 | 统一prompt框架 | [配置]`config.py:33` |
| 上下文理解 | 有限（函数级） | 较强（CCI三维度分析） | `prompts.py:56-84` |

#### 具体对比点

**传统工具优势**：
1. 规则可解释性强（[待填]误报/漏报具体案例）
2. 无API依赖，离线可用
3. 检测结果稳定（确定性输出）

**本项目优势**：
1. 语义级理解：CCI能分析代码变更的深层意图
2. RAG类比推理：能发现变体漏洞
3. 无需预定义规则：新漏洞类型可被识别

### 3.2 优点分析

| 优点 | 具体表现 | 来源依据 |
|------|----------|----------|
| 语义级分析 | CCI三维度分析（Summary/Purpose/Implications） | `prompts.py:56-84` |
| RAG增强 | 831条历史漏洞知识支撑检索 | [实测] |
| 双入口设计 | 支持patch/diff格式与原始代码 | `my_main.py:8-19`, `my_main.py:71-95` |
| 评估体系完整 | 4项指标（Precision/Recall/F1/MCC） | `check_point.py:28-36` |
| 交互工具 | CLI工具支持直接代码分析 | `tool.py:20-52` |

### 3.3 缺点分析

| 缺点 | 具体表现 | 来源依据 |
|------|----------|----------|
| API完全依赖 | 需DashScope API Key | `config.py:10` |
| 输出不确定性 | 同代码多次分析可能不同 | LLM固有特性 |
| RAG检索局限 | Top_K=1，错误先例可能误导 | `config.py:34` |
| Recall过低 | 仅30.8%漏洞被检出 | [实测] |
| 成本与延迟 | 向量化+LLM调用双重开销 | [待填]实际耗时数据 |

---

## 四、分析报告

### 4.1 AI局限性

#### 4.1.1 理解局限性

**结论**：LLM对复杂代码逻辑的上下文理解仍有限，导致漏报。

**依据**：
- [实测] Recall=0.3077，13个漏洞中仅检测出4个
- [代码]`prompts.py:124-161` CAVFD prompt要求"provide evidence"判断是否为漏洞修复，但模型仍遗漏

**待分析**：需人工审查FN样本，确认是语义理解不足还是其他原因

#### 4.1.2 输出不确定性

**结论**：LLM输出具有随机性，同一代码多次分析可能得到不同结论。

**依据**：
- [代码]`utils.py:77` `inference_llm`函数无温度参数控制
- [实测] fin.csv中存在`"likely"`等模糊判断（34条），而非明确的yes/no

**待补充**：重复实验验证一致性

#### 4.1.3 资源与效率

| 问题 | 具体表现 | 数据来源 |
|------|----------|----------|
| API调用成本 | qwen-turbo按token计费 | [配置]`config.py:15` |
| 向量化延迟 | text-embedding-v4批量处理 | `utils.py:54-65` |
| 速率限制 | NVD API要求`NVD_API_DELAY=1s` | [配置]`config.py:49` |

**待补充**：实际运行耗时数据

#### 4.1.4 格式解析失败

**结论**：`check_point.py`使用简单字符串匹配`"yes"/"no"`，可能因LLM输出格式变化而失效。

**依据**：[代码]`check_point.py:12-13`

### 4.2 改进方法

#### 4.2.1 提升检测精度

| 方法 | 预期效果 | 实现位置 |
|------|----------|----------|
| 增加Top_K检索 | 获取更多候选案例减少偏差 | `config.py:34` 改为Top_K=3 |
| 多模型融合 | 综合SA+CAVFD结果判决 | `my_main.py:71-95` 已有但未充分融合 |
| Prompt优化 | 针对高漏报类型专项调优 | `prompts.py` |

**待补充**：具体调优实验数据

#### 4.2.2 鲁棒性增强

| 方法 | 具体做法 | 代码依据 |
|------|----------|----------|
| 输出格式校验 | JSON解析失败时重试或降级 | `my_main.py:85-92` |
| 模糊结果处理 | `"likely"`应计入特定类别 | `check_point.py:12-13`仅处理yes/no |
| 专家反馈循环 | 人工标注FN/FP样本用于few-shot | [待实现] |

#### 4.2.3 效率优化

| 方法 | 预期提升 | 待补充数据 |
|------|----------|------------|
| LLM Response缓存 | 避免重复API调用 | `utils.py:68-80`已有cache_dir参数 |
| 并行化API调用 | 加快批量处理 | `my_main.py`使用apply串行 |
| 增量RAG更新 | 无需全量重建 | `my_build_rag.py`全量构建 |

---

## 五、总结

### 5.1 项目成果

| 成果 | 量化指标 | 来源 |
|------|----------|------|
| 完整Pipeline | CCI+RAG+CAVFD三阶段 | [代码] |
| RAG知识库 | 831条Java CVE记录 | [实测] |
| 评估体系 | 4项指标完整 | [实测] |
| CLI工具 | 支持代码/文件输入 | [代码]`tool.py` |

### 5.2 关键创新

1. **CCI三维度分析框架**：将代码变更解析为Summary/Purpose/Implications
2. **RAG增强漏洞检测**：利用历史漏洞向量库实现类比推理
3. **双入口设计**：支持patch/diff格式与原始代码

### 5.3 实验结论

| 指标 | 值 | 评价 |
|------|-----|------|
| Precision | 0.80 | 较好 |
| Recall | 0.31 | 较差，需大幅提升 |
| F1 | 0.44 | 中等 |
| MCC | 0.49 | 中等 |

### 5.4 未来工作

- [ ] 扩展多语言支持（C/C++、Python）
- [ ] 提升Recall（目标>0.7）
- [ ] 引入更多评估指标（ROC、AUC）
- [ ] 探索微调领域专用模型
- [ ] 研究与传统静态分析工具的混合架构

---

## 附录

### A. 评估指标计算公式

```python
Precision = TP / (TP + FP)
Recall = TP / (TP + FN)
F1 = 2 × Precision × Recall / (Precision + Recall)
MCC = (TP×TN - FP×FN) / √((TP+FP)(TP+FN)(TN+FP)(TN+FN))
```

### B. 关键代码片段索引

| 功能 | 文件:行号 |
|------|-----------|
| CCI生成（patch） | `utils.py:83-88` |
| CCI生成（代码） | `utils.py:92-97` |
| RAG检索 | `utils.py:135-143` |
| CAVFD判断 | `utils.py:113-121` |
| 主流程 | `my_main.py:61-67` |
| 评估逻辑 | `check_point.py:7-24` |

### C. 待补充空缺清单

1. FN样本（9条漏报）的详细分析
2. 实际运行耗时数据
3. 重复实验一致性验证
4. 跨语言泛化实验
5. 误报（FP）具体案例分析
