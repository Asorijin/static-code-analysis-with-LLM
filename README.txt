CCI部分原本是分析代码更改意图，需要更改为分析代码执行意图，即这段代码是做什么的。
HV部分是将历史已查明的漏洞嵌入向量数据库，作为静态分析的查询库，不用改动，但是需要找对应语言的已知漏洞，可以去nvd找。需要包括CVE ID，CVE描述等

整体执行流程是：

HV前置工作：找已知漏洞，输入至CCI组件，让LLM分析上述三个角度，输出为文本，处理为向量数据库

执行CCI：分析输入的代码执行意图，传输至LLM分析上述三个角度，输出为文本，嵌入为向量
执行HV：将上一步嵌入得到的向量在向量数据库中查找（目的是通过代码相似度检出可能的类似漏洞）

最后将原代码，原代码的CCI分析，HV检索出的相似漏洞，HV检索漏洞的CCI分析整合进prompt，输入至LLM，判定是否为漏洞及判断原因

输入结构：
[testing_codebase]
输出结构：
[tesing_codebase,cavfd]

向量数据库结构：
三角度分析原文，三角度分析嵌入向量，历史漏洞原信息[CVEID,语言,原代码]

项目结构：
my_main:主处理流程
my_build_rag:构建向量数据库流程
prompt:LLM提示词（需要大改）
utils:文本分割工具（原本用于去除GitHub提交信息，不适用于本项目（大概））
checkout_point:工具指标检测